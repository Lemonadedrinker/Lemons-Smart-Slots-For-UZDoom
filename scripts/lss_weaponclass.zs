class LSS_Weapon
{
    // The weapon object itself
    ui Weapon weapon;

    // The slot that the weapon is saved in (by the mod, not by default)
    ui int slot;

    // The slot's priority
    ui int row;
}